name: pricing-env
description: Dynamic pricing and multi-period inventory management environment

endpoints:
  reset:
    method: POST
    path: /reset

  step:
    method: POST
    path: /step

  state:
    method: GET
    path: /state

models:
  action: PricingAction
  observation: PricingObservation
  state: PricingState